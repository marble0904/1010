using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NumberController : MonoBehaviour {

    public int[,] intValues = new int[4, 4]; 

	// Use this for initialization
	void Start () {
		for(int i=0; i<4; i++)
        {
            for(int j = 0; j < 4; j++)
            {

            }
        }
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
